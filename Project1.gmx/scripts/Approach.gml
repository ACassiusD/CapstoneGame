/// Approach(start, end, shift);


if (argument0 < argument1) //If start is less than end
    //Return lower between start+shift and end;
    return min(argument0 + argument2, argument1);  
                                                      
else //if start is greater than end 
    //Return highest between start-shift and end
    return max(argument0 - argument2, argument1);
